### 5. 関係変更ファイル
* [FieldModal.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/FieldModal.tsx)（コース選択状態・UI・結果文字列組み立て処理を拡張）
* [ControlPanel.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/ControlPanel.tsx)（複数パーツの修飾子スキャナー＆日本語正順マッピングロジックの実装）

---

## 🚀 2026-05-26 追加改修内容6：アウト詳細モーダルのSVG野球場グラフィックUI化 ＆ ファウルアウト「邪」トグル新設

ユーザー様からの「アウト詳細モーダルもインプレイ（FieldModal）と同様に美しくグラフィカルなUIにしたい」「ファウルフライ（邪飛）などを正しく日本の野球スコア表記で記録したい」という素晴らしいご提案を解決するための大刷新を完了しました。

### 1. プレミアムSVG野球場グラフィックUIの完全移植
* `OutDetailModal.tsx` 内の無機質だったポジションボタンの格子グリッドを排除し、`FieldModal.tsx` で大好評の**芝生と土が精密に描かれた美しいSVG野球場UIを100%移植・全面導入**しました！
* ダイヤモンド上の 1〜9 の標準守備位置（P, C, 1B, 2B, 3B, SS, LF, CF, RF）にバッジボタンを物理配置。
* 指でタップしたポジションには、光が鼓動するようにアニメーションする「赤いピンマーク」がリアルタイムにプロットされ、操作の楽しさとプレミアム感を大幅に高めました。

### 2. 「ファウル（邪）」トグルスイッチの新設
* フライ（`FO`）またはライナー（`LO`）を選択した際に、自動でスライド展開する **「打球エリア（ファウルグラウンド判定）」トグルスイッチ** をモーダル内に新設しました。
* これをONにすると、結果文字列に `"FOUL"` 修飾子を自動付与して組み立てを行います（例: `"2-FOUL-FO"`）。
* これにより、あらゆるポジションにおける「ファウルフライ」や「ファウルライナー」の詳細な状態を記録できるようになりました！

### 3. コントロールパネルの `handleOutResult` パーサー拡張 ＆ 正順マッピング
* コントロールパネル側のパーサーを拡張し、ハイフン接続文字列から `"FOUL"` 修飾子を正確に抽出・検出するロジックを実装しました。
* 日本の公式野球スコアブックの表記規則に100%則った、美しい日本語マッピングと結合順序を実現しました：
  * **結合順序**: `[ポジション日本語名]` + `[邪 (ファウル時)]` + `[飛 / 直 / ゴロ などのアウト結果]`
  * **マッピング結果例**:
    * 捕手 `(2)` をタップ ＋ フライアウト `(FO)` ＋ ファウル `(FOUL)` ➜ **`「捕邪飛」`** (キャッチャーファウルフライ)！
    * 三塁手 `(5)` をタップ ＋ ライナー `(LO)` ＋ ファウル `(FOUL)` ➜ **`「三邪直」`** (サードファウルライナー)！
    * レフト `(7)` をタップ ＋ フライアウト `(FO)` ＋ ファウル `(FOUL)` ➜ **`「左邪飛」`**！
    * 遊撃手 `(6)` をタップ ＋ ゴロアウト `(GO)` ➜ **`「遊ゴロ」`** (通常のフェアグラウンドアウト)

### 4. 既存システムとの完全な互換性・安全性
* 生成される結果文字列は、アウト数カウントやイニング交代処理を司る `ScoreContext.tsx` 内のロジックとの完全な前方互換性を保っているため、システムに不具合を与えることなく安全に稼働します。

### 5. 関係変更ファイル
* [OutDetailModal.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/OutDetailModal.tsx)（SVG野球場UI、タップピン状態、ファウルエリアトグルスイッチ、ハイフン接続結果組み立てを実装）
* [ControlPanel.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/ControlPanel.tsx)（`handleOutResult` のパーサーリファクタリング、`FOUL` 修飾子の抽出・マッピングを実装）

---

## 🚀 2026-05-26 追加改修内容7：インプレイモーダルへの「バント」性質オプション新設 ＆ バント安打・バント野選完全対応

ユーザー様からの「送りバントやセーフティバントが成功してセーフ（安打や野選）になった際、バントが選択できないため記録がつけられない」という実戦に即した極めて重要なご指摘に基づき、インプレイ出塁モーダル（`FieldModal`）を拡張し、バント出塁の完全記録に対応しました！

### 1. インプレイモーダルへの「バント」オプションの追加
* `FieldModal.tsx` 内の「打球の性質（オプション）」のボタングループ（ゴロ、フライ、ライナー）に、新しく **`「バント」`**（ID: `"BUNT"`) ボタンを新設・追加しました。
* `trajectory` 状態の型定義を `"GO" | "FO" | "LO" | "BUNT" | null` へ安全に拡張。
* バントをタップして決定（確認）すると、自動的に `"BUNT"` 修飾子を含んだハイフン接続結果が組み立てられます（例：`"1-BUNT-1B"` や `"5-BUNT-1B"`、`"1-BUNT-FC"`）。

### 2. コントロールパネルの `handleFieldResult` マッピングパーサーの拡張
* ポジションありの打球性質 `trajMap` に **`"BUNT": "バ"`** を追加しました。これにより、守備位置と結果の間に自動的に「バ」が挟まれ、野球の公式スコア表記を100%忠実に再現します。
* ポジションなし（クイック記録）の `trajMap` に **`"BUNT": "バント"`** を追加しました。
* **出力・記録される日本語スコア表現**:
  * ピッチャーへバントして単打でセーフ（セーフティバント成功） ➜ **`「投バ安」`**（バント安打 / `1-BUNT-1B`）！
  * サードへバントして単打でセーフ ➜ **`「三バ安」`**（`5-BUNT-1B`）！
  * ピッチャーへバントして相手が野選でセーフ ➜ **`「投バ選」`**（バント野選 / `1-BUNT-FC`）！
  * ファーストへバントして相手のエラーでセーフ ➜ **`「一バ失」`**（バントでの相手エラー出塁 / `3-BUNT-E`）！
  * ポジション指定なしのクイック記録 ➜ **`「バント単打」`**（`BUNT-1B`）！

### 3. 既存の全システムとの完璧な互換性・安全性
* バントによる出塁結果であっても、結果文字列の末尾が必ず `"安"`, `"選"`, `"失"` などの判定キーで終わるため、既存の進塁自動シミュレーションや、アウトカウント判定、交代システムを一切破壊することなく安全に動作します！

### 4. 関係変更ファイル
* [FieldModal.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/FieldModal.tsx)（定数配列およびステート型定義に `BUNT` を追加。UIへバントボタンを配置）
* [ControlPanel.tsx](file:///c:/Users/mamor/OneDrive/ドキュメント/GitHub/i-score/src/components/score/ControlPanel.tsx)（`handleFieldResult` 内の打球性質マッピングに `BUNT` ➜ `"バ"` / `"バント"` を実装）
